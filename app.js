const buttons=document.querySelectorAll('.service');const toast=document.getElementById('toast');const cont=document.getElementById('continue');buttons.forEach(b=>b.onclick=()=>{buttons.forEach(x=>x.classList.remove('active'));b.classList.add('active')});cont.onclick=()=>{const s=document.querySelector('.service.active').dataset.name;toast.textContent=`Servicio seleccionado: ${s}. El siguiente paso se conectará con n8n.`;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3500)};